"use client";

// Tamil pipeline paused 2026-06-27 — toggle hidden until Tamil is re-enabled.
// To restore: remove the early return below and uncomment the original toggle logic.

export function LanguageToggle({ dark = false }: { dark?: boolean }) {
  void dark;
  return null;
}
